PROMPT — Reversão do fluxo inline (c3a3cee) + orquestração via startExecution.sync
============================================================================

Nenhuma das duas opções. Decisão de arquitetura: o fluxo inline do commit
c3a3cee está ERRADO e será desfeito. A arquitetura correta é a
orquestração-mãe chamar a SFN agnóstica via startExecution.sync em cada
fase — a agnóstica é quem roda PreparaMetadados, o ciclo do arquivo e os
Maps. O trabalho na agnóstica/prepara-metadados/executoras está correto e
será usado.

Plano, nesta ordem:

1. Na MÃE: mostrar como desfazer o c3a3cee com segurança (git revert na
   develop vs. reimplementar em branch feature por cima), listando o que do
   inline vale reaproveitar (ex.: sequência de status por fase, Catch por
   fase). Restaurar: variável sfn_orquestracao_lote_arn no variables.tf e
   policy IAM com states:StartExecution na agnóstica + permissões do .sync
   (events), removendo os lambda:InvokeFunction diretos.

2. Na AGNÓSTICA: adicionar o modo tipo_arquivo=JSONL por prefixo com
   LOAD_AND_FLATTEN (hoje só há CSV/JSON/PARQUET) — é o que a fase 2 da
   extração precisa. Manter o contexto_lote já implementado na branch.

3. Na MÃE (novo definition.asl.json):
   - FASE 1 chama a agnóstica com pedido.csv (CSV, colunas
     identificacao;agrupamento;tipoPessoa) + contexto_lote {idExtracao,
     idLoteDisponibilidade, filtros} + executora-disponibilidade; atualiza
     status PASSO_1.
   - FASE 2 chama a agnóstica com o prefixo
     disponibilidades/{id}/itens-extracao/ (JSONL/LOAD_AND_FLATTEN) +
     contexto_lote {idExtracao, idLoteExtracao} + executora-extracao;
     status PASSO_2.
   - FASE 3 Glue ZIPar; status PASSO_3.
   - Catch por fase → status FALHA com a fase identificada.

4. Apresentar o desenho (ASL comentado) e o plano de commits por repo ANTES
   de implementar.

--------------------------------------------------------------------------
ADENDO — Esclarecimento sobre os dois Glue jobs
--------------------------------------------------------------------------

São coisas distintas e ambos permanecem como estão em seus lugares.

- O Glue de ingestão DENTRO da agnóstica é genérico (ingere os
  dados/registros de qualquer lote que passou pela step, sem conhecimento
  de domínio) e roda normalmente nas duas fases.
- O Glue ZIPar é ESPECÍFICO do processo de extração em lote e existe apenas
  na orquestração-mãe, como fase própria após a fase 2.

Não condicionar, mover ou unificar nenhum dos dois.

PROMPT — CSVDelimiter: confirmação na doc AWS + Choice estático COMMA/SEMICOLON
============================================================================

Verifiquei na documentação oficial da AWS
(docs.aws.amazon.com/step-functions/latest/dg/input-output-itemreader.html):

1. CSVDelimiter EXISTE no ReaderConfig — valores aceitos: COMMA (default),
   PIPE, SEMICOLON, SPACE, TAB. O SEMICOLON herdado é válido.

2. ReaderConfig NÃO aceita JSONPath dinâmico (.$) — é configuração
   estática; só Parameters/Arguments aceitam dinâmico. Portanto:
   implemente o item 3 com Choice roteando para variantes estáticas
   COMMA/SEMICOLON dos Maps CSV (arquivo e diretório), decidindo pelo
   csv_delimiter detectado/declarado nos metadados — mesmo padrão do
   Choice de tipo de arquivo existente.

3. LOAD_AND_FLATTEN também confirmado na doc oficial (listObjectsV2 +
   InputType + Transformation) — pode manter o desenho da fase 2 como
   está.

Mantenha as camadas aprovadas: detecção na prepara-metadados > override
explícito > default COMMA. Pode implementar.
